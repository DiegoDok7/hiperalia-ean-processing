# Configuración para APIs
# Reemplaza los valores de ejemplo con tus API keys reales

# === APIs de IA (configura al menos una) ===
# OpenAI API Key
OPENAI_API_KEY = "tu-api-key-openai-aqui"

# Claude API Key (Anthropic)
CLAUDE_API_KEY = "tu-api-key-claude-aqui"

# Proveedor de IA por defecto: "openai" o "claude"
DEFAULT_AI_PROVIDER = "openai"

# === APIs de datos ===
# EANSearch API Token (opcional - mejora límites de uso)
EANSEARCH_API_TOKEN = "tu-token-eansearch-aqui"

# === Instrucciones ===
# Para obtener tu API key de OpenAI:
# 1. Ve a https://platform.openai.com/api-keys
# 2. Crea una nueva API key
# 3. Copia la key y pégala arriba

# Para obtener tu API key de Claude:
# 1. Ve a https://console.anthropic.com/
# 2. Crea una nueva API key
# 3. Copia la key y pégala arriba

# Para obtener tu token de EANSearch (opcional):
# 1. Ve a https://www.ean-search.org/ean-database-api.html
# 2. Regístrate para obtener un token con más límites
# 3. Copia el token y pégalo arriba

# IMPORTANTE: 
# - Este archivo es solo un ejemplo
# - NUNCA subas config.py con API keys reales a Git
# - El archivo config.py está en .gitignore por seguridad 